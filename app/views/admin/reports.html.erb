
<h4>PRE-APPROVAL</h4>
<%= render file: "admin/rooms" %>
<br>
<h4>ENTRY WITH CONFIRMED BETTER VERSION</h4>
<%= render 'table' %>
<br>
<h4>SPAM/STATUS REPORTS</h4>
<ul class="nav nav-tabs">
  <li class="active"><a data-toggle="tab" href="#menu">NO LONGER EXIST</a></li>
  <li><a data-toggle="tab" href="#menu1">UNDER RENOVATION</a></li>
  <li><a data-toggle="tab" href="#menu2">INAPPROPRIATE CONTENT</a></li>
  <li><a data-toggle="tab" href="#menu3">SPAM</a></li>
</ul>

<div class="tab-content">
  <div id="menu" class="tab-pane fade in active">
	<% @reports = Report.no_longer_exist.paginate(:page => params[:page], :per_page => 2) %>
	<%= render 'report_table' %>
  </div>
  <div id="menu1" class="tab-pane fade">
    <% @reports = Report.under_renovation.paginate(:page => params[:page], :per_page => 2) %>
	<%= render 'report_table' %>
  </div>
  <div id="menu2" class="tab-pane fade">
    <% @reports = Report.inappropriate_content.paginate(:page => params[:page], :per_page => 2) %>
	<%= render 'report_table' %>
  </div>
  <div id="menu3" class="tab-pane fade">
  </div>
</div>
